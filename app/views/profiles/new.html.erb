<%= simple_form_for @profile do |f| %>
  <%= f.input :picture %>
  <%= f.input :first_name %>
  <%= f.input :description %>
  <%= f.button :submit, value: "Create Profile", class: "btn btn-large btn-primary" %>
<% end %>